# ============================================================
# VulnBank – application.properties
# ============================================================

spring.datasource.url=jdbc:mysql://localhost:3306/vulnbank_db
spring.datasource.username=root
spring.datasource.password=vulnbank123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8080

# VB-008 | CWE-798 | Hardcoded JWT secret in config file
jwt.secret=vulnbank-hardcoded-secret-9876

# VB-015 | CWE-200 | Sensitive Endpoint Exposure – all actuator endpoints enabled
management.endpoints.web.exposure.include=*
management.endpoint.env.enabled=true
management.endpoint.heapdump.enabled=true
management.endpoint.shutdown.enabled=true

spring.profiles.active=vulnerable
